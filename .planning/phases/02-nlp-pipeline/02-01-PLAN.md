---
phase: 02-nlp-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pipeline/__init__.py
  - pipeline/stages/__init__.py
  - pipeline/stages/embed.py
  - pipeline/stages/topic_model.py
  - pipeline/stages/vibe_score.py
  - pipeline/stages/sentiment.py
  - pipeline/stages/temporal.py
  - pipeline/stages/export.py
  - pipeline/archetypes.json
  - pipeline/PIPELINE_DECISIONS.md
  - scripts/06_run_nlp_pipeline.py
  - tests/conftest.py
  - tests/test_embed.py
  - tests/test_topic_model.py
  - tests/test_vibe_score.py
  - tests/test_sentiment.py
  - tests/test_temporal.py
  - tests/test_faiss_index.py
  - tests/test_quotes.py
  - tests/test_artifacts.py
  - requirements-nlp.txt
autonomous: true
requirements:
  - NLP-09

must_haves:
  truths:
    - "Pipeline orchestrator script exists and parses CLI args (--db, --artifacts-dir, --force, --force-stage)"
    - "All 6 pipeline stage modules exist with stub functions and artifact gating"
    - "Archetype seed phrases config file exists with all 6 vibe dimensions"
    - "Test scaffolds exist with failing tests for every NLP requirement"
    - "conftest.py provides shared fixtures for mock embeddings, mock DB, mock topics"
  artifacts:
    - path: "scripts/06_run_nlp_pipeline.py"
      provides: "Pipeline orchestrator entry point"
      contains: "argparse"
    - path: "pipeline/archetypes.json"
      provides: "Vibe archetype seed phrases"
      contains: "artsy"
    - path: "pipeline/stages/embed.py"
      provides: "Embedding stage stub"
      contains: "def run_embed"
    - path: "tests/conftest.py"
      provides: "Shared test fixtures including NLP phase fixtures"
      contains: "mock_embeddings"
  key_links:
    - from: "scripts/06_run_nlp_pipeline.py"
      to: "pipeline/stages/*.py"
      via: "import and sequential stage execution"
      pattern: "from pipeline.stages"
---

<objective>
Create the pipeline project structure, archetype configuration, orchestrator skeleton, and Wave 0 test scaffolds for all NLP requirements.

Purpose: Establish the foundation that all subsequent plans build on -- directory structure, interfaces, configuration, and failing tests that define acceptance criteria for each pipeline stage.
Output: Runnable orchestrator (stubs only), archetype config, NLP dependency file, complete test scaffolds with fixtures.
</objective>

<execution_context>
@/home/cx3429/.claude/get-shit-done/workflows/execute-plan.md
@/home/cx3429/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-nlp-pipeline/02-CONTEXT.md
@.planning/phases/02-nlp-pipeline/02-RESEARCH.md
@.planning/phases/02-nlp-pipeline/02-VALIDATION.md

<interfaces>
<!-- Phase 1 SQLite schema (from scripts/02_build_schema.py) -->
```sql
CREATE TABLE IF NOT EXISTS businesses (
    business_id        TEXT PRIMARY KEY,
    name               TEXT NOT NULL,
    latitude           REAL,
    longitude          REAL,
    neighbourhood_id   TEXT,
    neighbourhood_name TEXT,
    city               TEXT,
    state              TEXT,
    attributes         TEXT
);

CREATE TABLE IF NOT EXISTS reviews (
    review_id   TEXT PRIMARY KEY,
    business_id TEXT NOT NULL,
    text        TEXT NOT NULL,
    stars       INTEGER NOT NULL,
    review_date TEXT NOT NULL,
    source      TEXT NOT NULL DEFAULT 'yelp',
    useful      INTEGER,
    funny       INTEGER,
    cool        INTEGER,
    FOREIGN KEY (business_id) REFERENCES businesses(business_id)
);
```

<!-- Phase 1 _log pattern (from scripts/04_ingest_reviews.py) -->
```python
_ANSI = {"WARN": "\033[93m", "FAIL": "\033[91m", "INFO": ""}
_RESET = "\033[0m"

def _log(level: str, msg: str) -> None:
    ts = datetime.datetime.utcnow().strftime("%Y-%m-%d %H:%M:%S")
    color = _ANSI.get(level, "") if sys.stdout.isatty() else ""
    reset = _RESET if color else ""
    print(f"{color}[{level}] {ts}  {msg}{reset}", flush=True)
```

<!-- Artifact directory and file manifest (from 02-CONTEXT.md) -->
All artifacts in `data/output/artifacts/`:
- embeddings.npy (n_reviews, 384) float32
- review_ids.npy (n_reviews,) int64
- bertopic_model/ (safetensors)
- topic_assignments.json {review_id: topic_id}
- vibe_scores.json {neighbourhood_id: {archetype: score}}
- temporal_series.json {neighbourhood_id: {year: {archetype: score}}}
- faiss_index.bin (FAISS flat index binary)
- faiss_id_map.json (FAISS int ID -> neighbourhood_id)
- representative_quotes.json {neighbourhood_id: {archetype: [quote, ...]}}
- sentiment_model/ (merged DistilBERT weights)
- enriched_geojson.geojson
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pipeline structure, orchestrator, archetype config, and NLP dependencies</name>
  <files>
    pipeline/__init__.py,
    pipeline/stages/__init__.py,
    pipeline/stages/embed.py,
    pipeline/stages/topic_model.py,
    pipeline/stages/vibe_score.py,
    pipeline/stages/sentiment.py,
    pipeline/stages/temporal.py,
    pipeline/stages/export.py,
    pipeline/archetypes.json,
    pipeline/PIPELINE_DECISIONS.md,
    scripts/06_run_nlp_pipeline.py,
    requirements-nlp.txt
  </files>
  <read_first>
    scripts/04_ingest_reviews.py,
    scripts/02_build_schema.py,
    .planning/phases/02-nlp-pipeline/02-CONTEXT.md,
    .planning/phases/02-nlp-pipeline/02-RESEARCH.md
  </read_first>
  <action>
    1. Create `requirements-nlp.txt` with exact pinned versions:
       ```
       sentence-transformers==5.3.0
       bertopic==0.17.4
       peft==0.18.1
       faiss-cpu==1.13.2
       hdbscan==0.8.41
       umap-learn
       datasets
       accelerate
       safetensors
       ```

    2. Create `pipeline/__init__.py` (empty, just a docstring: `"""NLP pipeline for neighbourhood vibe scoring."""`).

    3. Create `pipeline/stages/__init__.py` that exports all stage functions:
       ```python
       from pipeline.stages.embed import run_embed
       from pipeline.stages.topic_model import run_topic_model
       from pipeline.stages.vibe_score import run_vibe_score
       from pipeline.stages.sentiment import run_sentiment
       from pipeline.stages.temporal import run_temporal
       from pipeline.stages.export import run_export
       ```

    4. Create each stage module with a consistent interface. Every stage module MUST:
       - Have a `run_<stage>(db_path: str, artifacts_dir: Path, force: bool = False) -> dict` function
       - Start with artifact existence check: `if output_path.exists() and not force: _log("INFO", f"...skipping"); return {"skipped": True}`
       - Use the same `_log(level, msg)` pattern from Phase 1 (copy from 04_ingest_reviews.py)
       - Return a dict with stage metadata (e.g., `{"skipped": False, "count": N}`)
       - Body is `raise NotImplementedError("Stage not yet implemented")` after the artifact check

       Specific function signatures:
       - `embed.py`: `run_embed(db_path: str, artifacts_dir: Path, force: bool = False) -> dict` -- checks for `artifacts_dir / "embeddings.npy"`
       - `topic_model.py`: `run_topic_model(db_path: str, artifacts_dir: Path, force: bool = False) -> dict` -- checks for `artifacts_dir / "bertopic_model"`
       - `vibe_score.py`: `run_vibe_score(db_path: str, artifacts_dir: Path, force: bool = False) -> dict` -- checks for `artifacts_dir / "vibe_scores.json"`
       - `sentiment.py`: `run_sentiment(db_path: str, artifacts_dir: Path, force: bool = False) -> dict` -- checks for `artifacts_dir / "sentiment_model"`
       - `temporal.py`: `run_temporal(db_path: str, artifacts_dir: Path, force: bool = False) -> dict` -- checks for `artifacts_dir / "temporal_series.json"`
       - `export.py`: `run_export(db_path: str, artifacts_dir: Path, force: bool = False) -> dict` -- checks for `artifacts_dir / "enriched_geojson.geojson"`

    5. Create `pipeline/archetypes.json`:
       ```json
       {
         "artsy": ["art gallery", "creative space", "street art", "indie music", "bohemian", "artist studio", "mural", "craft workshop"],
         "foodie": ["amazing food", "best restaurant", "culinary experience", "farm to table", "food scene", "brunch spot", "chef", "tasting menu"],
         "nightlife": ["great bar", "nightclub", "live music venue", "cocktail bar", "late night", "DJ", "happy hour", "pub crawl"],
         "family": ["kid friendly", "family restaurant", "playground nearby", "safe neighborhood", "family owned", "children welcome", "stroller friendly"],
         "upscale": ["fine dining", "luxury", "upscale boutique", "high end", "exclusive", "elegant", "premium", "sophisticated"],
         "cultural": ["museum", "historic landmark", "cultural center", "heritage", "diversity", "ethnic cuisine", "tradition", "community festival"]
       }
       ```

    6. Create `scripts/06_run_nlp_pipeline.py` orchestrator:
       - argparse with `--db` (default: `data/output/reviews.db`), `--artifacts-dir` (default: `data/output/artifacts`), `--force` (reruns all stages), `--force-stage` (reruns named stage: embed, topic_model, vibe_score, sentiment, temporal, export)
       - Imports all 6 stage functions from `pipeline.stages`
       - Defines `STAGES` ordered list: `[("embed", run_embed), ("topic_model", run_topic_model), ("vibe_score", run_vibe_score), ("sentiment", run_sentiment), ("temporal", run_temporal), ("export", run_export)]`
       - Iterates stages in order, passes `force=True` if `--force` or `--force-stage` matches
       - Uses `_log()` for progress output
       - Creates `artifacts_dir` if not exists
       - Wraps each stage in try/except, logs errors, exits on failure

    7. Create `pipeline/PIPELINE_DECISIONS.md` documenting technical choices:
       - BERTopic over LDA: semantic embeddings vs bag-of-words
       - sentence-transformers over word2vec: contextual embeddings for short text
       - LoRA fine-tuning over VADER: domain adaptation, portfolio differentiator
       - FAISS over brute-force numpy: industry standard, optimized C++
       - Philadelphia pivot: 157 neighbourhoods, Yelp data 2005-2022
       - Temporal range: using actual data years (not aspirational 2019-2025)
  </action>
  <verify>
    <automated>python -c "from pipeline.stages import run_embed, run_topic_model, run_vibe_score, run_sentiment, run_temporal, run_export; print('imports OK')" && python scripts/06_run_nlp_pipeline.py --help</automated>
  </verify>
  <acceptance_criteria>
    - pipeline/__init__.py exists and contains "NLP pipeline"
    - pipeline/stages/__init__.py contains "from pipeline.stages.embed import run_embed"
    - pipeline/stages/embed.py contains "def run_embed(db_path: str, artifacts_dir: Path, force: bool = False) -> dict"
    - pipeline/stages/topic_model.py contains "def run_topic_model("
    - pipeline/stages/vibe_score.py contains "def run_vibe_score("
    - pipeline/stages/sentiment.py contains "def run_sentiment("
    - pipeline/stages/temporal.py contains "def run_temporal("
    - pipeline/stages/export.py contains "def run_export("
    - pipeline/archetypes.json contains "artsy" and "foodie" and "nightlife" and "family" and "upscale" and "cultural"
    - scripts/06_run_nlp_pipeline.py contains "--force-stage" and "--artifacts-dir" and "from pipeline.stages"
    - pipeline/PIPELINE_DECISIONS.md contains "BERTopic" and "LoRA" and "FAISS"
    - requirements-nlp.txt contains "sentence-transformers==5.3.0" and "bertopic==0.17.4" and "peft==0.18.1" and "faiss-cpu==1.13.2"
    - `python -c "from pipeline.stages import run_embed"` exits 0
  </acceptance_criteria>
  <done>All 6 stage modules importable with consistent interfaces. Orchestrator parses CLI args. Archetype config has all 6 dimensions. NLP dependencies pinned.</done>
</task>

<task type="auto">
  <name>Task 2: Create Wave 0 test scaffolds and shared fixtures for all NLP requirements</name>
  <files>
    tests/conftest.py,
    tests/test_embed.py,
    tests/test_topic_model.py,
    tests/test_vibe_score.py,
    tests/test_sentiment.py,
    tests/test_temporal.py,
    tests/test_faiss_index.py,
    tests/test_quotes.py,
    tests/test_artifacts.py
  </files>
  <read_first>
    tests/conftest.py,
    .planning/phases/02-nlp-pipeline/02-VALIDATION.md,
    .planning/phases/02-nlp-pipeline/02-RESEARCH.md,
    pipeline/stages/embed.py
  </read_first>
  <action>
    1. Update `tests/conftest.py` -- ADD (do not remove existing Phase 1 fixtures) the following new fixtures:

       a. `mock_db_with_reviews(tmp_path)` -- Creates a temp SQLite DB with Phase 1 schema, inserts 3 businesses (neighbourhood_ids "044", "116", "121", neighbourhood_names "Fishtown - Lower Kensington", "Rittenhouse", "Society Hill") and 30 reviews per business (90 total, 10 per neighbourhood per year 2019-2021), review text is synthetic but varied (e.g., "Great food and atmosphere in Fishtown", "Lovely cafe in Rittenhouse"). Returns the DB path string.

       b. `mock_embeddings(tmp_path)` -- Creates `embeddings.npy` shape (90, 384) with random float32 values (seeded np.random.seed(42)), and `review_ids.npy` shape (90,) with ints 1-90. Returns the tmp artifacts directory Path.

       c. `mock_artifacts_dir(tmp_path)` -- Returns an empty `tmp_path / "artifacts"` directory (created).

       d. `archetypes_path()` -- Returns `Path("pipeline/archetypes.json")` (the real config file).

    2. Create `tests/test_embed.py` (NLP-01):
       - `test_embeddings_shape(mock_db_with_reviews, mock_artifacts_dir)` -- Calls `run_embed(db_path, artifacts_dir)`, asserts `embeddings.npy` shape is `(N, 384)` where N > 0, dtype is float32.
       - `test_review_ids_alignment(mock_db_with_reviews, mock_artifacts_dir)` -- Asserts `review_ids.npy` length equals embeddings row count.
       - `test_embed_skip_existing(mock_embeddings)` -- With embeddings already present, asserts `run_embed()` returns `{"skipped": True}` without re-computing.

    3. Create `tests/test_topic_model.py` (NLP-02):
       - `test_topic_count(mock_db_with_reviews, mock_embeddings)` -- Asserts BERTopic produces >= 20 topics (mark `@pytest.mark.slow`).
       - `test_outlier_rate_below_50_pct(mock_db_with_reviews, mock_embeddings)` -- Asserts outlier rate < 0.5 after `reduce_outliers()` (mark `@pytest.mark.slow`).
       - `test_topic_model_saves(mock_artifacts_dir)` -- Asserts `bertopic_model/` directory exists after stage runs (mark `@pytest.mark.slow`).
       - `test_topic_assignments_json(mock_artifacts_dir)` -- Asserts `topic_assignments.json` is valid JSON with review_id keys.
       Note: For the non-slow unit tests, use small synthetic data. The slow tests will run against real pipeline output.

    4. Create `tests/test_vibe_score.py` (NLP-03, NLP-05):
       - `test_vibe_scores_six_dimensions(mock_artifacts_dir)` -- Asserts every neighbourhood in `vibe_scores.json` has exactly 6 keys: artsy, foodie, nightlife, family, upscale, cultural.
       - `test_vibe_scores_vary(mock_artifacts_dir)` -- Asserts not all neighbourhood scores are identical (standard deviation > 0 for at least one dimension).
       - `test_recency_weighting_log_space()` -- Unit test: given review dates and a half-life of 365 days, asserts weights computed via log-space match expected values within 1e-6 tolerance. Asserts minimum weight clamp of 1e-6.
       - `test_recency_weight_clamp()` -- Asserts that a review 10 years old gets weight clamped to 1e-6, not 0.

    5. Create `tests/test_sentiment.py` (NLP-04):
       - `test_lora_config_valid()` -- Asserts LoraConfig(task_type="SEQ_CLS", r=16, lora_alpha=32, lora_dropout=0.1, target_modules=["q_lin", "v_lin"]) does not raise.
       - `test_merged_model_loadable(mock_artifacts_dir)` -- Asserts `sentiment_model/` can be loaded with `AutoModelForSequenceClassification.from_pretrained()` (mark `@pytest.mark.slow`).
       - `test_three_class_output()` -- Asserts model output has num_labels=3.

    6. Create `tests/test_temporal.py` (NLP-06):
       - `test_temporal_series_structure(mock_artifacts_dir)` -- Asserts `temporal_series.json` has neighbourhood_id keys, each with year keys, each with 6 archetype scores.
       - `test_temporal_no_nan(mock_artifacts_dir)` -- Asserts no NaN values in any temporal score.
       - `test_temporal_all_neighbourhoods(mock_db_with_reviews, mock_artifacts_dir)` -- Asserts every neighbourhood_id from DB appears in temporal series.

    7. Create `tests/test_faiss_index.py` (NLP-07):
       - `test_faiss_query_returns_k(mock_artifacts_dir)` -- Asserts querying with k=5 returns exactly 5 results.
       - `test_faiss_query_latency(mock_artifacts_dir)` -- Asserts query completes in < 10ms.
       - `test_faiss_id_map_matches(mock_artifacts_dir)` -- Asserts `faiss_id_map.json` has same count as FAISS index `ntotal`.

    8. Create `tests/test_quotes.py` (NLP-08):
       - `test_quotes_per_neighbourhood(mock_artifacts_dir)` -- Asserts 3-5 quotes per neighbourhood per archetype.
       - `test_quote_max_length(mock_artifacts_dir)` -- Asserts every quote is <= 300 characters.
       - `test_quotes_all_archetypes(mock_artifacts_dir)` -- Asserts all 6 archetypes present per neighbourhood.

    9. Create `tests/test_artifacts.py` (NLP-09):
       - `test_all_artifacts_exist(mock_artifacts_dir)` -- Asserts all 11 artifact files/directories exist in artifacts_dir: embeddings.npy, review_ids.npy, bertopic_model/, topic_assignments.json, vibe_scores.json, temporal_series.json, faiss_index.bin, faiss_id_map.json, representative_quotes.json, sentiment_model/, enriched_geojson.geojson.
       - `test_artifacts_loadable(mock_artifacts_dir)` -- Asserts each artifact loads without error (np.load for npy, json.load for json, faiss.read_index for bin).

    All test files should import from `pipeline.stages` and use the shared conftest fixtures. Tests that require full ML execution should be marked `@pytest.mark.slow`. Fast unit tests should use mock/synthetic data and run in < 30 seconds total.
  </action>
  <verify>
    <automated>pytest tests/test_embed.py tests/test_topic_model.py tests/test_vibe_score.py tests/test_sentiment.py tests/test_temporal.py tests/test_faiss_index.py tests/test_quotes.py tests/test_artifacts.py --collect-only 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/conftest.py contains "mock_db_with_reviews" and "mock_embeddings" and "mock_artifacts_dir"
    - tests/test_embed.py contains "test_embeddings_shape" and "test_review_ids_alignment" and "test_embed_skip_existing"
    - tests/test_topic_model.py contains "test_topic_count" and "test_outlier_rate_below_50_pct" and "pytest.mark.slow"
    - tests/test_vibe_score.py contains "test_vibe_scores_six_dimensions" and "test_recency_weighting_log_space" and "test_recency_weight_clamp"
    - tests/test_sentiment.py contains "test_lora_config_valid" and "test_three_class_output"
    - tests/test_temporal.py contains "test_temporal_no_nan" and "test_temporal_all_neighbourhoods"
    - tests/test_faiss_index.py contains "test_faiss_query_returns_k" and "test_faiss_query_latency"
    - tests/test_quotes.py contains "test_quotes_per_neighbourhood" and "test_quote_max_length"
    - tests/test_artifacts.py contains "test_all_artifacts_exist" and "test_artifacts_loadable"
    - `pytest tests/test_embed.py tests/test_vibe_score.py --collect-only` exits 0 and shows collected tests
  </acceptance_criteria>
  <done>All 8 test files created with named test functions covering NLP-01 through NLP-09. conftest.py has shared fixtures for mock DB, mock embeddings, and mock artifacts directory. Tests are discoverable by pytest --collect-only.</done>
</task>

</tasks>

<verification>
- `python -c "from pipeline.stages import run_embed, run_topic_model, run_vibe_score, run_sentiment, run_temporal, run_export"` exits 0
- `python scripts/06_run_nlp_pipeline.py --help` shows all CLI flags
- `pytest tests/test_embed.py tests/test_topic_model.py tests/test_vibe_score.py tests/test_sentiment.py tests/test_temporal.py tests/test_faiss_index.py tests/test_quotes.py tests/test_artifacts.py --collect-only` shows all test functions collected
- `cat pipeline/archetypes.json | python -m json.tool` validates JSON
</verification>

<success_criteria>
Pipeline project structure established. Orchestrator parses CLI. All 6 stage stubs importable. Archetype config valid. 8 test files with named tests covering all NLP requirements discoverable by pytest.
</success_criteria>

<output>
After completion, create `.planning/phases/02-nlp-pipeline/02-01-SUMMARY.md`
</output>
